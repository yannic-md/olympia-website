name: Run Jest Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '21' # Updated to Node.js 21 for compatibility

      - name: Install dependencies
        run: npm ci

      - name: Run tests with coverage
        run: npm test

      - name: Check coverage threshold
        run: |
          # Jest with coverageThreshold will automatically fail if coverage < 100%
          # This step will only run if the previous step succeeds
          echo "Coverage check passed - 100% coverage achieved!"
